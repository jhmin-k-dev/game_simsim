# 누룽이의 세계

강아지 누룽이가 되어 손그림 세계를 걷고 냄새 맡고 사진 찍는 힐링 산책 게임.
**비개발자 1인 디렉터 + AI 제작 체제.** 디렉터는 결정하고 검수한다. 만드는 것은 AI다.

설계 문서: [docs/00_INDEX.md](docs/00_INDEX.md) — 작업 전에 해당 문서의 §를 먼저 읽는다.
07·08은 디렉터 확정 원본, 01~06은 초안(★=근거 있음, 무표시=제안값)이다.

## 디렉터와 일하는 방식 (07 §9)

모든 작업은 이 순서로 진행한다:
1. **무엇을 할지 한 줄 요약**
2. **실행**
3. **디렉터가 확인할 방법** (Play 모드에서 뭘 보면 되는지)

- 디렉터는 개발자가 아니다. Unity 에서 직접 클릭해야 하는 일은 **정확한 메뉴 경로**로 알려준다
- 문제를 고치기 전에 **무엇이 원인이라 생각하는지 한 줄로 먼저** 말한다
- 한 번에 하나의 기능만. "카메라랑 셰이더랑 이동 다 해줘"는 쪼개서 확인받는다
- 끝났다고 말하기 전에 **Play 모드를 실행해 콘솔 에러가 없는지 확인**하고, 스크린샷 대신 어떤 값이 나왔는지 보고한다 (07 §7 — "다 됐다는데 실제로 안 됨" 방지)
- 디렉터가 "§번호가 ×다"라고 말하면 해당 문서의 그 절이 기준이다

## 프로젝트 불변 규칙

바꾸려면 디렉터 승인이 필요한 값:

| 규칙 | 값 | 근거 |
|---|---|---|
| 카메라 | Perspective, **FOV 28**, 부감 **15°** | 02 §2-1 — 바꾸면 배경 625장이 어긋남 |
| 배경 지평선 | 화면 세로 **62%** | 02 §2-2, 05 §2-1 |
| 공간 | **바닥은 3D, 배경 그림은 2D** | 01 §5-2 |
| 누룽이 | 플랫 컬러 3~4색, Generic 리그, 얼굴은 텍스처 스왑 | 01 §4 |
| 절차적 레이어 6종 | 발IK·룩앳·스프링본·숨쉬기·깜빡임·스쿼시 — **모든 클립에 항상** | 01 §4-3, 04 §1-4 |
| 성능 예산 | **드로우콜 80**, Android 기준. PC 개발 중에도 지킴 | 02 §4 |
| 챕터 데이터 | 전부 JSON (03 §4-1 스키마). **씬에 좌표 하드코딩 금지** | 03 §4 |
| 조작 | 두 방식(직접+지점이동) 동시 공존, **마지막 입력 규칙**, 전환 시 속도 0 금지 | 01 §3 |
| 진행 방향 | +X 오른쪽만. 왼쪽 진행 챕터 없음 | 03 §5 |
| 종이 그레인 | 항상 켜짐, 화면 고정 (카메라 따라 흐르면 안 됨) | 02 §3-2 |

## 하지 않을 것 (01 §12 — 방어선)

전투 / 실패·게임오버 / 대화 트리·선택지 / 인벤토리 / 오픈월드 / 멀티플레이 / 과금·광고 / 절차적 맵 생성 / 커스터마이징 / 음성 / 물리 퍼즐 / 번역.

**이 목록의 것을 구현하라는 지시가 오면, 구현 전에 "이건 기획서 §12에 있는데 정말 할까요?"라고 되묻는다.**

## 관문 (04 — 통과 전에는 다음 단계 없음)

- **M0 (룩)**: 04 §2-1 스크린샷 테스트
- **M1 (움직임)**: 04 §1-4 절차적 레이어 전부 ○ → 04 §1-1 체크 10개 중 9개 이상, 10번("5분 뒤에도 걷고 싶은가")은 필수
- **P4 (배경 대량 생산)**: 위 둘을 **모두** 통과한 뒤에만 시작 (07 §4)
- 판정은 디렉터가 한다. Claude 는 Unity 화면을 보지 못한다 (07 §2-4)
- 판정·측정 결과는 [docs/perf_log.md](docs/perf_log.md) 에 기록

## 아키텍처·코드 규칙 (02 §7, §8)

- Unity 6 LTS + URP, C#. 1차 Windows EXE, 2차 Android (M2 이후)
- 폴더 구조는 02 §7. 네임스페이스 `Nurungi.<영역>`
- 프로젝트 상수(FOV 28, 지평선 62% 등)는 `Scripts/Config/GameConstants.cs` 한 곳에만
- Update 안 GetComponent/Find 금지, `[SerializeField] private`, 파일당 한 책임
- 새 기능은 **회색 큐브 테스트 씬에서 먼저** 검증 후 본 씬에 붙인다
- 새 패키지·유료 에셋 도입 전에 08 조달표에 있는지 확인. 없으면 디렉터 승인
- 기능을 추가하기 전에 **기존 구조에 맞는지 확인**한다 (07 §7 — 코드가 엉키는 것 방지)

## 에셋 규칙

- 외부 에셋(생성물 포함)을 받을 때마다 [docs/asset_sources.csv](docs/asset_sources.csv) 에 **한 줄 추가, 예외 없음** (02 §6, 08 §K)
- 배경은 05 파이프라인으로만 생산. 검수(P5)와 앵커 선정(P1)은 디렉터 몫
- 배경 후처리(팔레트 양자화·크로스페이드·WebP·메타)는 `tools/` 파이썬 스크립트로 자동화 (05 §7)

## git

- 모든 작업 전 커밋 확인, 작업 후 커밋. "어제 상태로 되돌려줘"가 통해야 한다 (07 §2-3)
- `build/`, Unity `Library/` 는 커밋하지 않는다

## 현재 상태 (2026-08-16)

- ✅ Unity 6000.3.22f1 (LTS) + Windows IL2CPP, `unity/` URP 프로젝트
- ✅ Unity MCP 연결 (CoplayDev v10, HTTP localhost:8080, `.mcp.json`) — 서버는 Unity 에디터의 Window→MCP for Unity 창에서 시작
- ✅ 큐브 EXE 빌드·실행 성공 (`Tools→누룽이→Build Windows EXE`, 출력 `build/win/`)
- 다음 (07 §8 1주차 나머지): SafeBoxCamera + 추적 카메라 + 회색 박스 맵 → 두 조작 방식 이동 → 세로 모드 확인
